export enum Somethings {
  FIRST_THING = "first-things",
  SECOND_THING = "second-thing",
}

export enum PostTypes {
  NEWS = "news",
  EVENTS = "events",
  PROJECTS = "projects",
  PUBLICATIONS = "publications",
}

export enum ContactTypes {
  PHONE = "phone",
  EMAIL = "email",
}